## 1. Component Updates

- [x] 1.1 Update `StatGridItemProps` to include `baseSpeed: number`.
- [x] 1.2 Refactor `StatGridItem.tsx` to include the `baseSpeed` display and update the Tailwind column spans.
- [x] 1.3 Refactor `TierSection.tsx` to include the "Base" header label and update the grid header column spans.

## 2. Verification

- [x] 2.1 Run `npm run type-check` to ensure no prop errors were introduced.
- [x] 2.2 Verify the UI renders correctly with the new "Base" column aligned with the tier values.
