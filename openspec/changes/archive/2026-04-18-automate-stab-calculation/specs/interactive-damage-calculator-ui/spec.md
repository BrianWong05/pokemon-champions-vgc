## MODIFIED Requirements

### Requirement: Reactive Stat Inputs
The UI SHALL update the damage results immediately upon any stat, slider, Pokémon selection, or move type change.

#### Scenario: Changing move type
- **WHEN** the user selects a new type for the attacker's move
- **THEN** the damage results SHALL update immediately, reflecting whether STAB is newly applied or removed.
