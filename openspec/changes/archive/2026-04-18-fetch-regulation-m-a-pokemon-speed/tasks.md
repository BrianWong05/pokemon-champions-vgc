## 1. Setup

- [x] 1.1 Ensure `src/services/pokemon.ts` directory exists or create the file.
- [x] 1.2 Import necessary Drizzle ORM functions and schema definitions.

## 2. Implementation

- [x] 2.1 Implement the `fetchRegulationMAPokemonSpeed` function using Drizzle to query for Pokémon legal in 'Regulation M-A'.
- [x] 2.2 Add speed benchmark calculation logic within the `map` phase of the data fetch.
- [x] 2.3 Implement sorting by `baseSpeed` in descending order.

## 3. Verification

- [x] 3.1 Create a test script (or use an existing one) to verify the function returns the expected number of Pokémon and correct speed stats.
