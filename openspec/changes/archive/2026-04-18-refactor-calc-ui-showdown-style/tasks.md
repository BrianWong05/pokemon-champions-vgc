## 1. Atom & Molecule Refactor

- [x] 1.1 Refactor `StatControlGroup` to use a row-based CSS grid.
- [x] 1.2 Implement the "Total" calculated stat display in each row.
- [x] 1.3 Make base stats read-only and remove input styling to prevent manual editing.

## 2. Organism Refactor

- [x] 2.1 Update `AttackerPanel` header to include `TypeBadge` and better name/dropdown layout.
- [x] 2.2 Update `DefenderPanel` header to include `TypeBadge` and better name/dropdown layout.
- [x] 2.3 Implement the "Total SP" counter at the bottom of the stat grid in both panels.

## 3. Integration & Verification

- [x] 3.1 Ensure all stat changes correctly propagate to the damage calculation.
- [x] 3.2 Verify grid alignment across different screen sizes.
