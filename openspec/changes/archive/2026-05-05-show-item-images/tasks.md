## 1. Asset Management & Utilities

- [x] 1.1 Create `getItemImageUrl` utility function to map item names to `/items` paths.
- [x] 1.2 Implement filename normalization logic (e.g., "Choice Band" -> "Choice_Band_SV.png").

## 2. Component Implementation

- [x] 2.1 Create an `ItemImage` component to encapsulate image rendering and fallback logic.
- [x] 2.2 Update `PokemonPanel.tsx` to include the `ItemImage` next to the item selection dropdown.
- [x] 2.3 Ensure the image correctly updates and handles the "None" or empty item state.

## 3. UI/UX Polishing

- [x] 3.1 Adjust layout spacing in `PokemonPanel` to accommodate the new image smoothly.
- [x] 3.2 Verify image rendering for items with special characters (dashes, quotes).
