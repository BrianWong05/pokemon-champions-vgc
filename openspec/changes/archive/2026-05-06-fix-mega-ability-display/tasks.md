## 1. Investigation

- [x] 1.1 Trace the ability fetching logic in `usePokemonEditor` and see how it relates to Pokémon form IDs.
- [x] 1.2 Confirm if `pokemon_abilities` table supports form-based ability lookups.

## 2. Implementation

- [x] 2.1 Update `handleSelectPokemon` in `usePokemonEditor` to correctly fetch abilities for the selected form ID if applicable.
- [x] 2.2 Test the fix by adding Mega Pokémon to a team and verifying ability display in the editor.
