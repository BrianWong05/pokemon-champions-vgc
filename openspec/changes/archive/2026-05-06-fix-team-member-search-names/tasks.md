## 1. Investigation

- [x] 1.1 Verify why `PokemonSearchSelect` is not rendering Pokémon names in its dropdown.
- [x] 1.2 Inspect `PokemonSearchSelect.tsx` and its mapping logic.

## 2. Fix

- [x] 2.1 Apply fix to correctly map or render Pokémon names.
- [x] 2.2 Verify that the fix resolves the display issue in `TeamDetailPage`.
