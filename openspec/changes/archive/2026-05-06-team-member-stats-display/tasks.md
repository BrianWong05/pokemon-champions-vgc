## 1. UI Implementation

- [x] 1.1 Create a compact `TeamMemberStatDisplay` component that renders stat rows for a given `PokemonConfig`
- [x] 1.2 Update the member card component in `TeamDetailPage` to include the `TeamMemberStatDisplay` component
- [x] 1.3 Adjust card layout to ensure stats are clearly presented without making the card overly tall
