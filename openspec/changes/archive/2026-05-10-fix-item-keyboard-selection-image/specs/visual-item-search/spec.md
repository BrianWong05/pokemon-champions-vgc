## ADDED Requirements

### Requirement: Item Images in Search Results
The system SHALL display the corresponding item image next to each item name in the search results dropdown.

#### Scenario: Verify item images
- **WHEN** the item search dropdown is open
- **THEN** each row MUST contain an icon/image representing that item.
