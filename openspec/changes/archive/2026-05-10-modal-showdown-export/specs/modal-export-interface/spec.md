## ADDED Requirements

### Requirement: Showdown Export Modal
The system SHALL display the exported Showdown-formatted string within a modal dialog rather than directly copying to the clipboard.

#### Scenario: Verify modal export
- **WHEN** a user clicks "Export"
- **THEN** the system MUST open a modal containing the exported text, providing a manual "Copy to Clipboard" button.
