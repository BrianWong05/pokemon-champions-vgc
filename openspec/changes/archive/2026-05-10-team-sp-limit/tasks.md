## 1. UI Updates

- [x] 1.1 Add `enforceSpLimit` prop to `StatGrid` and `PokemonConfigForm`
- [x] 1.2 Update `StatGrid` to apply SP limit visual cues conditionally

## 2. Integration

- [x] 2.1 Pass `enforceSpLimit` from `PokemonPanel` and `TeamMemberEditorModal`

## 3. Review

- [x] 3.1 Verify SP limit logic across Damage Calculator and Team Editor
